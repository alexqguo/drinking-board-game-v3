{
  "extends": "@repo/typescript-config/base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src", // do not ever change this, it will dump all output into src randomly
    "declarationDir": "dist-types"
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist", "**/*.d.ts"]
}
